<article>
  <header>
    <h1>Saiba tudo que acontece no Meetup Ember BR</h1>
  </header>

  <PostsSection @posts={{this.model.allPosts}} />

  <aside>
    <h2>Conheça nossos autores</h2>
    <ul>
      {{#each this.model.allAuthors as |author|}}
        <li>
          <LinkTo @route="authors.author" @model={{author.id}}>
            {{author.name}}
          </LinkTo>
        </li>
      {{/each}}
    </ul>
  </aside>
</article>
