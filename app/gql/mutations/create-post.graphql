mutation CreatePost($id: ID!, $title: String!, $body: String!, $views: Int!, $author_id: ID!) {
  post: createPost(id: $id, title: $title, body: $body, views: $views, author_id: $author_id) {
    id
  }
}
